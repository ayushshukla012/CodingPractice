package Java8Features;
@FunctionalInterface
// This is a functional interface, which means it has exactly one abstract method.
public interface OperationFunctionalInterface {
	int apply (int a, int b);
}
